import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  seperator: {
    width: 17,
  },
  listContainer: {
    marginStart: '2%',
    marginBottom: '2%',
  },
  cardHeight: {
    height: 160,
  },
});

export default styles;
